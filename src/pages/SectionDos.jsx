function latestWork(props) {
    const {title} = props
    return(
        
        <div className="last_work">
           <h2> {props.title} </h2> 
           
        </div>
    )
}

export default latestWork
